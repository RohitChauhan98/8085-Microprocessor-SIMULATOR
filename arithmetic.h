#ifndef ARITHMETIC_H
#define ARITHMETIC_H
#include <string>

using namespace std;

class Arithmetic
{
public:
    void ADD(std::string address, int *register, int A);
    void SUB(std::string address, int *register, int A);
};

#endif
